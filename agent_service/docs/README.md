# Agent Service Docs

Put operational runbooks here or upload them through `POST /api/agent/upload`.
The rewritten service indexes uploaded files with the local fallback knowledge
store and can use their snippets in chat or AI operations reports.
