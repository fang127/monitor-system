# monitor-system 统一运行时配置。
# 模块内的局部配置文件保留给独立开发使用，本文件作为 compose 和宿主机侧 manager 运行的默认入口。

# MySQL 配置，供 docker compose 和宿主机侧 manager 使用。
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_DATABASE=monitor-system

# Worker MySQL 实例监控配置。默认关闭；需要采集 MySQL 运行指标的 worker 主机可设为 true。
MYSQL_MONITOR_ENABLED=false
MYSQL_MONITOR_HOST=127.0.0.1
MYSQL_MONITOR_PORT=3306
MYSQL_MONITOR_USER=monitor
MYSQL_MONITOR_PASSWORD=
MYSQL_MONITOR_INSTANCE=127.0.0.1:3306
MYSQL_MONITOR_ROLE=unknown
MYSQL_MONITOR_TIMEOUT_SECONDS=3
# 可选的多实例配置格式：
# MYSQL_MONITOR_TARGETS=instance|host|port|user|password|role;instance2|host2|3306|user|password|replica

# Worker Redis 实例监控配置。默认关闭；需要采集 Redis 运行指标的 worker 主机可设为 true。
REDIS_MONITOR_ENABLED=false
REDIS_MONITOR_HOST=127.0.0.1
REDIS_MONITOR_PORT=6379
REDIS_MONITOR_PASSWORD=
REDIS_MONITOR_INSTANCE=127.0.0.1:6379
REDIS_MONITOR_TIMEOUT_SECONDS=3
# 可选的多实例配置格式：
# REDIS_MONITOR_TARGETS=cache1|127.0.0.1|6379|password;cache2|10.0.0.2|6379|password

# Redis 配置，供宿主机侧 manager 使用。
MANAGER_REDIS_ENABLED=true
MANAGER_REDIS_URI=tcp://127.0.0.1:6379

# Manager 调优参数。docker compose 默认不会启动 manager。
MANAGER_GRPC_ADDR=127.0.0.1:50051
MANAGER_GRPC_NUM_CQS=4
MANAGER_GRPC_MIN_POLLERS=4
MANAGER_GRPC_MAX_POLLERS=8
MANAGER_TASK_QUEUE_CAPACITY=10000
MANAGER_TASK_QUEUE_HIGH_WATERMARK=8000
MANAGER_TASK_QUEUE_LOW_WATERMARK=3000
MANAGER_TASK_TIMEOUT_MS=3000
MANAGER_INGEST_SHARD_COUNT=8
MANAGER_INGEST_QUEUE_CAPACITY_PER_SHARD=2048
MANAGER_QUERY_QUEUE_CAPACITY=10000
MANAGER_QUERY_QUEUE_HIGH_WATERMARK=8000
MANAGER_QUERY_QUEUE_LOW_WATERMARK=3000
MANAGER_QUERY_THREADS_MIN=4
MANAGER_QUERY_THREADS_MAX=16
MANAGER_QUERY_IDLE_SHRINK_SECONDS=30
MANAGER_BUSINESS_THREADS_MIN=4
MANAGER_BUSINESS_THREADS_MAX=16
MANAGER_BUSINESS_IDLE_SHRINK_SECONDS=30
MANAGER_MYSQL_WRITE_POOL_MIN=2
MANAGER_MYSQL_WRITE_POOL_MAX=8
MANAGER_MYSQL_QUERY_POOL_MIN=2
MANAGER_MYSQL_QUERY_POOL_MAX=8
MANAGER_MYSQL_CONNECT_TIMEOUT_MS=1000
MANAGER_MYSQL_READ_TIMEOUT_MS=2000
MANAGER_MYSQL_HEALTH_CHECK_SECONDS=10
MANAGER_MYSQL_IDLE_TTL_SECONDS=60
MANAGER_REDIS_POOL_MIN=2
MANAGER_REDIS_POOL_MAX=8
MANAGER_REDIS_CONNECT_TIMEOUT_MS=500
MANAGER_REDIS_READ_TIMEOUT_MS=1000
MANAGER_REDIS_HEALTH_CHECK_SECONDS=10
MANAGER_REDIS_IDLE_TTL_SECONDS=60
MANAGER_REDIS_CACHE_TTL_SECONDS=5
MANAGER_VERBOSE_LOG=false
MANAGER_METRICS_LOG_INTERVAL_SECONDS=10

# api_gateway 配置。Docker 使用 DOCKER_MANAGER_GRPC_ADDR 让容器访问宿主机侧 manager。
# MANAGER_HOST_GATEWAY 控制容器内的 manager-host 别名；原生 Linux Docker 可使用 host-gateway，
# Docker Desktop 无法通过 host.docker.internal 访问 manager 时可设置为 WSL/Linux IP。
API_GATEWAY_PORT=8080
API_GATEWAY_VERSION=v0.1.0
GIN_MODE=debug
MANAGER_GRPC_TIMEOUT=5s
MANAGER_HOST_GATEWAY=172.29.230.214 # 当前 WSL/Linux manager 监听地址；WSL IP 变化后需要同步更新
DOCKER_MANAGER_GRPC_ADDR=manager-host:50051
DOCKER_MYSQL_HOST=mysql # Docker compose 内 mysql 服务地址
JWT_SECRET=please-change-this-jwt-secret # 生产环境务必修改为复杂随机字符串
JWT_ACCESS_TTL=24h # JWT 访问令牌有效期
ADMIN_USERNAME=admin # 生产环境务必修改为更安全的管理员用户名
ADMIN_PASSWORD=please-change-this-admin-password # 生产环境务必修改为复杂随机密码

# agent_service 配置。Docker 专用地址指向 compose 服务。
AGENT_SERVICE_PORT=6872
API_GATEWAY_BASE_URL=http://127.0.0.1:8080
DOCKER_API_GATEWAY_BASE_URL=http://api_gateway:8080
MILVUS_ADDR=127.0.0.1:19530
DOCKER_MILVUS_ADDR=milvus:19530
AGENT_DOCS_DIR=./docs
AGENT_THINK_API_KEY=
AGENT_THINK_BASE_URL=
AGENT_THINK_MODEL=
AGENT_QUICK_API_KEY=
AGENT_QUICK_BASE_URL=
AGENT_QUICK_MODEL=
AGENT_EMBEDDING_API_KEY=
AGENT_EMBEDDING_MODEL=
AGENT_MEMORY_LONG_TERM_ENABLED=false # 长期记忆功能，开启后 agent 会将重要信息存储到 Milvus 向量数据库中，供后续查询使用。
AGENT_MEMORY_WRITE_ENABLED=false # 记忆写入功能，开启后 agent 会将对话中的重要信息写入 Milvus 向量数据库，供后续查询使用。通常与长期记忆功能配合使用。
AGENT_MEMORY_RECALL_ENABLED=false # 记忆召回功能，开启后 agent 会在对话中主动查询 Milvus 向量数据库
AGENT_MEMORY_RECENT_WINDOW=6 # 近期记忆窗口大小，agent 在对话中会优先考虑近期的上下文信息，单位为轮次。
AGENT_MEMORY_SUMMARY_WINDOW=12 # 记忆总结窗口大小，agent 会定期对对话进行总结，将重要信息提取并存储到长期记忆中，单位为轮次。
AGENT_MEMORY_RECALL_LIMIT=5 # 记忆召回限制，agent 在每轮对话中最多召回的记忆条数，过多可能导致上下文过长影响性能。
AGENT_MEMORY_TOKEN_BUDGET=1200 # 记忆令牌预算，agent 在每轮对话中用于记忆的最大令牌数，过多可能导致上下文过长影响性能。

# web 配置。
WEB_PORT=3000
