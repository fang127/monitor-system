## 1. 作用域与配置

- [x] 1.1 定义 `MemoryScope`、`MemoryPolicy`、`MemoryContext`、`MemorySelector` 等核心结构，覆盖租户、团队、集群和会话维度。
- [x] 1.2 新增长期记忆全局配置、召回数量、上下文预算、摘要窗口、写入开关和召回开关配置项。
- [x] 1.3 实现作用域策略合并逻辑，支持全局、租户、团队和集群配置按更具体作用域覆盖。

## 2. 记忆管理抽象

- [x] 2.1 新增 `MemoryManager` 接口，提供加载上下文、追加会话轮次、更新摘要、抽取长期记忆、设置开关和删除记忆能力。
- [x] 2.2 新增 `MemoryStore` 接口，隔离短期会话存储、长期记忆存储、策略存储和事件审计存储。
- [x] 2.3 提供内存兼容实现，确保长期记忆关闭时现有聊天接口仍可工作。

## 3. 持久化与向量索引

- [x] 3.1 新增 agent 自有记忆数据库表迁移，包含作用域策略、会话摘要、长期记忆和记忆事件表。
- [x] 3.2 新增长期记忆向量索引存储，使用独立 Milvus collection，避免与内部文档知识库混用。
- [x] 3.3 实现长期记忆创建、查询、更新状态、删除中标记、已删除标记和审计事件写入。
- [x] 3.4 实现向量索引写入、召回、删除和删除失败重试补偿。

## 4. 会话窗口与摘要

- [x] 4.1 将普通聊天和流式聊天从直接调用 `GetSimpleMemory` 迁移到 `MemoryManager.AppendTurn` 和 `MemoryManager.LoadContext`。
- [x] 4.2 实现最近消息窗口控制，保证注入模型的短期消息不超过配置上限。
- [x] 4.3 实现会话摘要更新流程，记录当前问题、已确认事实、已执行查询、助手结论和待处理事项。
- [x] 4.4 在长期记忆存储不可用时提供降级路径，保证聊天流程可继续运行。

## 5. 长期记忆写入与召回

- [x] 5.1 实现长期记忆候选抽取流程，仅在长期记忆写入开关启用后运行。
- [x] 5.2 实现候选记忆规则过滤，拒绝凭据、个人敏感信息、一次性临时状态和未确认猜测。
- [x] 5.3 实现长期记忆确认状态，支持 active、pending、deleted 和 deleting 等状态。
- [x] 5.4 实现长期记忆语义召回，按租户、团队和集群作用域过滤，并控制召回数量和 token 预算。
- [x] 5.5 在 prompt 输入中标记长期记忆的类型、作用域和置信度，避免模型把长期记忆误认为实时监控事实。

## 6. 管理 API

- [x] 6.1 新增长期记忆列表和详情接口，支持按租户、团队、集群、类型、状态和时间范围筛选。
- [x] 6.2 新增长期记忆开关接口，支持设置全局、租户、团队和集群作用域的写入与召回策略。
- [x] 6.3 新增长期记忆删除接口，支持按记忆 ID 删除。
- [x] 6.4 新增按作用域清空长期记忆接口，并写入删除审计事件。
- [x] 6.5 确保管理接口沿用现有鉴权机制，并校验调用方只能操作允许的作用域。

## 7. 聊天编排集成

- [x] 7.1 扩展聊天请求 DTO 或上下文解析逻辑，获取租户、团队、集群和会话标识。
- [x] 7.2 修改聊天 prompt 输入结构，支持固定指令、会话摘要、最近消息、长期记忆召回结果和当前用户问题。
- [x] 7.3 保持监控事实查询工具只通过 `api_gateway` 获取实时事实，不从记忆数据库读取实时监控事实。
- [x] 7.4 为普通聊天和流式聊天统一接入记忆加载、轮次追加、摘要更新和长期记忆候选抽取。

## 8. 测试与验证

- [x] 8.1 为作用域隔离、策略合并、开关行为和删除过滤添加单元测试。
- [x] 8.2 为会话窗口、会话摘要更新和聊天上下文组装添加单元测试。
- [x] 8.3 为长期记忆写入、召回、过期过滤、状态过滤和向量删除补偿添加测试。
- [x] 8.4 为普通聊天和流式聊天添加集成测试，覆盖长期记忆启用和关闭两种路径。
- [x] 8.5 运行 `PATH=/usr/local/go/bin:$HOME/go/bin:$PATH GOCACHE=/tmp/monitor-system-go-cache-agent go test ./...` 验证 `agent_service`。

## 9. 文档与迁移

- [x] 9.1 更新 `agent_service` README，说明记忆系统作用域、配置、管理接口和安全边界。
- [x] 9.2 更新架构文档，说明记忆数据库、向量索引和 `api_gateway` 监控事实边界。
- [x] 9.3 编写部署迁移说明，包含默认关闭长期记忆、数据库迁移、Milvus collection 初始化和回滚方式。
