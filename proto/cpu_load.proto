syntax = "proto3";
package monitor.proto;

// 表示不同时间窗口内的 CPU 平均负载。
message CpuLoad {
    float load_avg_1 = 1; // 1 分钟平均负载
    float load_avg_3 = 2; // 3 分钟平均负载
    float load_avg_15 = 3; // 15 分钟平均负载
}
